import styled from 'styled-components'

const StyledBans = styled.div`
  .banItem {
    padding: 10px 0;
    border-bottom: 1px solid #ffffff0f;
    cursor: pointer;
  }
  
`

export default StyledBans
