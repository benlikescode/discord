import styled from 'styled-components'

const StyledMessageTooLong = styled.div`

  text-align: center;
  margin-top: 20px;

  .header {
    margin-bottom: 8px;
    font-family: var(--font-display);
    font-weight: 600;
    color: #fff;
    font-size: 24px;
    line-height: 30px;
  }

  .message {
    color: #dcddde;
  }
  
`

export default StyledMessageTooLong
